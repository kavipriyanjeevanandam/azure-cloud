apiVersion: v1
kind: Service
metadata:
    name: kavipriyanjeevanandamazurecloud
spec:
    type: LoadBalancer
    ports:
    - port: 80 
    selector:
        app: kavipriyanjeevanandamazurecloud